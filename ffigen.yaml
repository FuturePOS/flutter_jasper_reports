# Run with `dart run ffigen --config ffigen.yaml`.
name: FlutterJasperReportsBindings
description: |
  Bindings for `src/flutter_jasper_reports.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/flutter_jasper_reports_bindings_generated.dart'
headers:
  entry-points:
    - 'src/flutter_jasper_reports.h'
  include-directives:
    - 'src/flutter_jasper_reports.h'
comments:
  style: any
  length: full
